---
description: Modularização incremental — extrair um módulo de cada vez do NonatoMainApp
alwaysApply: true
---

# Modularização por módulos (Nonato Service)

O monólito `app/NonatoMainApp.tsx` (~80k linhas) deve encolher por **extracções incrementais**, nunca por reescrita total.

## Procedimento (obrigatório)

1. **Um módulo por ciclo:** extrair → `npm run smoke:critico` → checklist de confiança → commit/push só com **SIM**.
2. **Só `StrReplace` / ficheiros novos** — nunca reescrever `NonatoMainApp.tsx` inteiro.
3. Extrair primeiro **funções puras + tipos**; UI/hooks só depois das dependências estarem no módulo.
4. Pasta padrão: `app/modules/<nome>/` com `index.ts` público.
5. Não misturar features novas com extracção no mesmo commit.
6. Actualizar `scripts/smoke-critico.mjs` quando surgir um novo módulo obrigatório.

## Ordem actual

1. `app/modules/fechamento/` (tarifas / linhas) — feito
2. `app/modules/clientes/` (alfabeto / detalhe) — feito
3. `app/modules/financeiro/` (devedores) — feito
4. `app/modules/orcamentos/` (workflow / avulso / equipamento) — feito
5. `app/modules/biblioteca/` (peças sync/merge/sequência + recovery) — feito
6. `app/modules/relatorios-especiais/` (tipos / cálculos / fechamento / PDF) — feito
7. `app/modules/comprovantes/` (OCR / duplicados / folha semanal) — feito
8. `app/modules/equipamentos/` (relatório / etiquetas armazém) — feito
9. `app/modules/relatorio-servico/` (dias / KM / numeração) — feito
10. `app/modules/agenda/` (tipos / normalize / datas / filtros / rótulos) — feito
11. `app/modules/financeiro/` (período / IVA) + `fechamento/iva` — feito
12. `app/modules/biblioteca/` (relatórios lista/árvore) — feito
13. `app/modules/sidebar/` (grupos / normalize / hub) — feito
14. `app/modules/diario/` (tipos / anexos / texto) — feito
15. `app/modules/fechamento/` (grupos de tarifa / migração legado) — feito
16. `app/modules/protocolo/` (tipos / blocos; SoT alinhado ao lib) — feito
17. `app/modules/biblioteca/` (catálogo clipboard / URL) — feito
18. `app/modules/financeiro/` (fluxo tipos / mutações) — feito
19. `app/modules/checklist/` (tipos / gerar mappers) — feito
20. `app/modules/contabilidade/` (print / HTML) — feito
21. `app/modules/biblioteca/` (import mappers / dedup) — feito
22. `app/modules/biblioteca/` (import parse plain/html) — feito
23. `app/modules/biblioteca/` (classificação) — feito
24. `app/modules/relatorio-servico/` (cálculos de dia) — feito
25. `app/modules/fechamento/` (cobrancaRelatorio) — feito
26. `app/modules/fechamento/` (exibirItens UI) — feito
27. `app/modules/agenda/` (estadoVisual UI) — feito
28. `app/modules/sst/` (tipos / form / mappers cliente) — feito
29. `app/modules/fechamento/` (persistMaps chaves/normalize) — feito
30. `app/modules/financeiro/` (fluxoNormalize mapa boot) — feito
31. `app/modules/relatorio-servico/` (pdfModelo keys/resolve) — feito
32. `app/modules/financeiro/` (faturaStatus sinal/agregado) — feito
33. `app/modules/financeiro/` (envioCobranca IBAN/texto) — feito
34. `app/modules/pdf/` (logos resolução PDF) — feito
35. `app/modules/sst/` (envioTexto / printPayload) — feito
36. `app/modules/admin/` (userForm vazio / mapeamento) — feito
37. `app/modules/comprovantes/` (periodo / lista datas) — feito
38. `app/modules/agenda/` (lembreteWhatsApp) — feito
39. `app/modules/biblioteca/` (display / busca UI) — feito
40. `app/modules/diario/` (compressImage) — feito
41. `app/modules/comprovantes/` (envioMensagem) — feito
42. `app/modules/sidebar/` (tabIntro / help / accent) — feito
43. `app/modules/sidebar/` (hubLabels / nav back) — feito
44. `app/modules/equipamentos/` (formState armazém) — feito
45. `app/modules/desmontados/` (tipos / form / migrate) — feito
46. `app/modules/admin/` (passwords tipo / generatePassword) — feito
47. `app/modules/idiomas/` (Language / getLanguages) — feito
48. `app/modules/pessoas/` (tipos / form / classes gestor-técnico) — feito
49. `app/modules/manuais/` (tipos grupo/modelo/documento) — feito
50. `app/modules/ficha-cadastral/` (tipos / empty / normalize) — feito
51. `app/modules/tradutor/` (TranslatorLibraryEntry / filter / match / normalize) — feito
52. `app/modules/conhecimento-tecnico/` (tipos / normalize / stats / create / filter) — feito
53. `app/modules/admin/` (LogoRelatorio / parse / preferRicher) — feito
54. `app/modules/demo/` (DemoModuleMode / destinatários / pickValid / countActive) — feito
55. `app/modules/fornecedores/` (Fornecedor / FaturaFornecedor / form / entidadeOrigem) — feito
56. `app/modules/comunicacao/` (MensagemComunicacao / PecaSolicitadaArmazem / identity / visibilidade) — feito
57. `app/modules/clientes/` (ClientePrioritario / form empty/toForm/create/update / completeness) — feito
58. `app/modules/orcamentos/` (PedidoOrcamento / buildFromRelatorio / temPecas) — feito
59. `app/modules/admin/` (User / UserPermissions / createUserFromForm / updateUserFromForm) — feito
60. `app/modules/biblioteca/` (PecaBiblioteca / CategoriaPeca / SubcategoriaPeca / createEmptyPecaBibliotecaForm) — feito
61. `app/modules/clientes/` (EquipamentoCliente / RelatorioEquipamento / createEmptyEquipamentoClienteForm) — feito
62. `app/modules/agenda/` (conflitoAgenda: técnico + cliente overlap) — feito
63. `app/modules/relatorio-servico/` (PecaSubstituicao / createEmptyPecaSubstituicaoForm) — feito
64. `app/modules/agenda/` (um em-andamento por técnico + cabeçalho real) — feito
65. `app/modules/relatorio-servico/` (createEmptyDiaTrabalhoForm) — feito
66. `app/modules/relatorio-servico/` (RelatorioEquipamentoRef / criarEquipamentoRelatorioVazio) — feito
67. `app/modules/relatorio-servico/` (RelatorioServico / createEmptyRelatorioServicoForm) — feito
68. `app/modules/relatorio-servico/` (ItemRelatorioExcluidoArquivo / Pasta / Storage) — feito

Próximos cortes (quando necessário): extrair mais UI/hooks destes módulos, ou novos domínios.

## Proibido neste processo

- Migrar JSX de ecrãs inteiros sem ter tipagem/utils no módulo
- Mover `translations.ts` / `globals.css` “por limpeza”
- Apagar código não relacionado no mesmo PR
