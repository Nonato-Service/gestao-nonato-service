/** Módulo admin — formulário de utilizadores (estado vazio / mapeamento). */

export type { UserFormState, UserForForm } from './userForm'
export { createEmptyUserForm, userToFormState } from './userForm'
